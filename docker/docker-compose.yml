version: "3.9"

services:
  ollama:
    build:
      context: ./ollamaimage
      dockerfile: Dockerfile
    container_name: ollamaserver
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    restart: unless-stopped
